/*
token {
   DOT;
   FROM;
   IDENTIFIER;
   LETTER;
   LITERAL;
   NUMBER;
   PARSE_ERROR;
   SCHEMA;
   SCHEMAS;
   SELECT;
   SEMI;
   SHOW;
   WHERE;
}*/

body {
    int my_counter;
}
constructor {
    self.my_counter = 0;
}
header {

#ifdef  QUEX_ACTION_TOKEN_STAMP
#   error "QUEX_ACTION_TOKEN_STAMP defined before!"
#endif
#define QUEX_ACTION_TOKEN_STAMP(ME)                 \
        QUEX_ACTION_TOKEN_STAMP_LINE_NUMBER(ME);    \
        QUEX_ACTION_TOKEN_STAMP_COLUMN_NUMBER(ME);  \
        QUEX_TOKEN_QUEUE_GET_CURRENT(ME)->number_ = self.my_counter++;   
}

mode X {
    [a-z] => BR_TKN_LETTER;
}
