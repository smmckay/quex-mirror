token_type {
   header {
#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>
   }
   standard {
        id            : uint32_t;
   }
   distinct {
       number :  size_t;
   }

   inheritable;

   copy {
        self.id    = Other.id;
        self.number = Other.number;
   }

   body {
   }

   take_text {
   }

   repetition_set {
   }

   repetition_get {
   }

   footer {
   }
}

