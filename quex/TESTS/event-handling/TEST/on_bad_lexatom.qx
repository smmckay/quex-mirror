// Errors: on_bad_lexatom;
// CHOICES: bad-utf16, good-utf16;
token { X; NEWLINE; }

mode X :
{
    .+ => TK_X;
    \n => TK_NEWLINE;

    on_bad_lexatom { 
        printf("bad_lexatom!\n");
        self_send(TK_TERMINATION);
    }
}

