// Customized 'on_load_failure' (only backward possible);
// CHOICES: empty, twice;

token { X; Y; }

mode X :
<skip_range: "/*" "*/">
<skip:       [ \t\n] >
{
    abcdefghijklmnopqrst/x/ => TK_X;
    x => TK_Y;
    on_load_failure { 
        printf("on_load_failure!");
    }
}

